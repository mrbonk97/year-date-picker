import { PickerPicker } from "./lib/picker/picker-picker";

function App() {
  return (
    <main>
      <PickerPicker type="YEAR" />
      <PickerPicker type="MONTH" />
    </main>
  );
}

export default App;
